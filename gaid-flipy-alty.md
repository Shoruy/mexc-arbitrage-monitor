# Гайд по флипам альту на аккаунтах MEXC с -PNL

## Зачем скупают аккаунты MEXC с -PNL?
При 0 комиссии на BTC, ETH — работает лимитками по стакану. На альтах — иначе.

## Суть
Binance двигается быстрее MEXC. Пока на MEXC цена не подтянулась — успеваем войти и забрать движение. Это не торговля, а поиск задержек между биржами.

## Сигналы
1. **Задержка Binance → MEXC**: Binance пошла, MEXC стоит — первый звонок.
2. **Разрыв фьюча монеты с BTC/ETH**: дополнительное подтверждение.
3. **Ликвидность**: проверять стакан перед входом.

## Монеты для флипа
XLM, Pengu, Aster, ZEC, XMR, XPL, BCH, HYPE

## Аккаунты MEXC
- Только с минусом (min -200$)
- Депозит до 30$ (не в стейблах)
- Покупка токена ~7$
- Торговля до получения РК

## Практика
1. Два стакана: MEXC и Binance (MetaScalp/Stakan.io).
2. Ждём разницу в цене.
3. Binance быстрее — на MEXC вход с задержкой до 2 сек.
4. Если на Binance цена ниже — шорт на MEXC, закрываем при схождении.

## Проблемы
- Прохождение РК
- Бан фьючей
- Убирают 0% комиссию
- Проверка источников дохода

---

## Техническая реализация (бот)

Проект: `mexc-tg-monitor.py` — монитор спреда Binance↔MEXC с авто-трейдингом и Telegram-алертами.

### Архитектура
- Binance: WebSocket bookTicker (реал-тайм, 8 пар)
- MEXC: REST polling каждую секунду (v3 bookTicker)
- Проверка ликвидности: v3 depth, лимит 20 стаканов
- Вход: спред 0.12%, выход: 0.02% или таймаут 30с
- Риск-контроль: 1 сделка одновременно, 20/день, стоп $3

### MEXC API migration (2026)
MEXC перевёл фьючерсные API на новый домен:

| Было | Стало |
|---|---|
| `api.mexc.com/api/v1/contract/detail` | `contract.mexc.com/api/v1/contract/detail` |
| `api.mexc.com/api/v1/private/*` | `contract.mexc.com/api/v1/private/*` |
| Символ `SOLUSDT` | `SOL_USDT` (через `_`) |

### Анти-бан (Cloudflare)
- aiohttp без User-Agent → CF блокирует
- Фикс: `_BROWSER_HEADERS` на каждый запрос
- Проверка Content-Type: 403 text/html → лог → graceful skip

### Параметры бота
- ENTRY_SPREAD = 0.12%, EXIT_SPREAD = 0.02%
- LEVERAGE = 20x, объём $7±$1
- Только аккаунты с -PNL (min -200$)
- Фильтр ликвидности: спред ≤0.05%, объём ≥$1000

### Файлы проекта
- `mexc-tg-monitor.py` — live-версия на VPS
- `mexc-spread-monitor.py` — старая версия (только сигналы)
- `mexc-spread-sim.py` — симуляция сделок с логами
